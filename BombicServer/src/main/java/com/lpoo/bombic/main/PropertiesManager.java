package com.lpoo.bombic.main;


public class PropertiesManager {

    //    private final Properties mProperties;

    public PropertiesManager(final String pPropertiesPath) {
        //        try {
        //            ...
        //        } catch (FileNotFoundException e) {
        //            LOGGER.warn("Properties file not found. Server shutting down", e);
        //            System.exit(0);
        //        } catch (IllegalArgumentException | IOException e) {
        //            LOGGER.warn("Couldn't read properties file. Server shutting down: ", e);
        //            System.exit(0);
        //        }
    }
}
